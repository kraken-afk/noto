[project]
name = "notos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2",
]

[dependency-groups]
dev = [
    "django-types>=0.20.0",
    "ruff>=0.11.5",
]
